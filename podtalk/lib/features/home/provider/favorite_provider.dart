// import 'package:flutter_riverpod/flutter_riverpod.dart';

// abstract class FavoriteNotifier extends Notifier<bool> {
//   FavoriteNotifier() : super(false);

//   void toggleFavorite() {
//     state = !state;
//   }
// }

// final favoriteProvider = NotifierProvider<FavoriteNotifier, bool>(
//   (ref) => FavoriteNotifier(false),
// );
